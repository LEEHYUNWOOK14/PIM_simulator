# B23 contract-integrity audit

B23 is sealed as a non-comparable exploratory result. Its generated route and smoke Tcl used `x0=1750` while the B21/B22 contract requires `x0=1500`; the y-origin=750 change was therefore not the sole independent variable. The B23 strict result (`residual=463`, `overflow_edges=351`) is preserved but is not used as gate evidence. B24 restores `x0=1500` and probes only `y0=1250`.
